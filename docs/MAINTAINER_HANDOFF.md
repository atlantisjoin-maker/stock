# 维护交接清单

- 当前稳定版本：4.1.0。
- 基础运行无第三方依赖。
- 行情源集中在 `src/astock_terminal/providers/`。
- 用户数据位于 `data/terminal.db`，配置位于 `config.json`。
- 发布包不得包含真实数据库、Token或Webhook。
- 修改证券代码映射时必须测试沪、深、北交所，尤其920代码。
- 新闻和基金报告不得用模型补全缺失数值。
- 双行情源未通过时新增仓位必须为0。
- 重大改动先增加数据库迁移和回滚说明。
